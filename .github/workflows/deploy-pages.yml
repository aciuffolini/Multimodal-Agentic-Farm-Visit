name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: |
          echo "=== INSTALLING ROOT DEPENDENCIES ==="
          npm install --legacy-peer-deps
          echo "✅ Root dependencies installed"
          echo "Verifying workspace structure..."
          ls -la packages/shared/ || echo "⚠️ packages/shared not found"
          ls -la apps/web/ || echo "⚠️ apps/web not found"

      - name: Build shared package
        run: |
          echo "=== BUILDING SHARED PACKAGE ==="
          npm run build:shared
          echo "✅ Shared package build command completed"

      - name: Verify shared build
        run: |
          echo "=== VERIFYING SHARED BUILD ==="
          if [ ! -d "packages/shared/dist" ]; then
            echo "❌ ERROR: packages/shared/dist directory not found!"
            echo "Contents of packages/shared:"
            ls -la packages/shared/
            exit 1
          fi
          echo "✅ Shared build verified"

      - name: Build web app
        working-directory: apps/web
        env:
          NODE_ENV: production
          VITE_BASE_PATH: /Multimodal-Agentic-Farm-Visit/
        run: |
          echo "=== BUILDING WEB APP (base=/Multimodal-Agentic-Farm-Visit/) ==="
          echo "VITE_BASE_PATH=$VITE_BASE_PATH"
          npm install --legacy-peer-deps  # Ensure local deps are ready
          npm run build
          echo "✅ Web app build command completed"

      - name: Verify web build
        run: |
          echo "=== VERIFYING WEB BUILD ==="
          if [ ! -d "apps/web/dist" ]; then
            echo "❌ ERROR: apps/web/dist directory not found!"
            ls -la apps/web/
            exit 1
          fi
          if [ ! -f "apps/web/dist/index.html" ]; then
            echo "❌ ERROR: apps/web/dist/index.html not found!"
            ls -la apps/web/dist/
            exit 1
          fi
          echo "✅ Web build verified"
      
      # FIX #6: Verify build size to catch empty outputs
      - name: Verify build artifact size
        run: |
          echo "=== VERIFYING BUILD SIZE ==="
          SIZE=$(du -sm apps/web/dist | cut -f1)
          echo "Dist size: ${SIZE} MB"
          if [ "$SIZE" -lt 1 ]; then
            echo "⚠️  WARNING: Build size is very small ($SIZE MB), may indicate build failure"
          else
            echo "✅ Build size: $SIZE MB (reasonable)"
          fi
      
      # Configure GitHub Pages (with enablement)
      - name: Setup Pages
        uses: actions/configure-pages@v4
        with:
          enablement: true
      
      # Upload artifact
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: apps/web/dist
          retention-days: 1

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    permissions:
      pages: write
      id-token: write
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
